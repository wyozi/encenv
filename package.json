{
  "name": "encenv",
  "version": "0.0.5",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": "./dist/cli.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "release:patch": "npm run clean && npm run build && npm version patch && npm publish"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.12",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "ajv": "^8.13.0",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  }
}
